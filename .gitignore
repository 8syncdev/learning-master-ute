# >>> 8sync (managed) >>>
# Derived / machine-local — rebuilt by `8sync harness init` + codegraph. Safe to ignore:
.codegraph/
.cache/8sync/
# Secrets — NEVER commit:
.env
.env.*
!.env.example
# KEEP COMMITTED (do NOT add here): agents/ (memory), agents/skills/, AGENTS.md, CHANGELOG.md
# <<< 8sync <<<

# Python virtual environment
.venv/
venv/
env/

# Python caches / build artifacts
__pycache__/
*.py[cod]
*.so
.ipynb_checkpoints/
.lock

# OS / editor cruft
.DS_Store
Thumbs.db

# Redundant submission archive (duplicate of the unpacked EDA folder)
*.zip

# LaTeX build artifacts (PDF bằng chứng đã copy vào folder nộp bài)
ml_ad/final/report/build/

# Demo artifacts (regenerable via save_artifacts.py)
ml_ad/final/demo/api/*.pt
ml_ad/final/demo/api/*.pkl
ml_ad/final/demo/web/node_modules/
ml_ad/final/demo/web/dist/
